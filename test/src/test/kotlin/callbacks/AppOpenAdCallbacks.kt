package callbacks

object AppOpenAdCallbacks {
    val loaded = "app open ad loaded"
    val impression = "app open ad impression"
    val shown = "app open ad showed"
    val clicked = "app open ad clicked"
    val dismissed = "app open ad dismissed"
}
