package callbacks

class BannerCallbacks {
    companion object {
        val loaded = "banner ad loaded"
        val impression = "impression"
        val clicked = "banner ad clicked"
        val leftApp = "left app"
        val returnedToApp = "returned to app"
    }
}
